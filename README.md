# trader
This Machine Learning Trading model uses HistGradientBoostingClassifier to perdict both 1 hour and 1 day price trends and puts them into states that the model can recognize. 

Currently It fetches 2 years of previous data and trains it into the model, where it is normalized

Currently both models have around 44% accuracy but it has not been tuned heavily
